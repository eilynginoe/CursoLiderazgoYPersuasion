---
// Parte server-side (build time)
const numero = `${import.meta.env.VITE_WHATSAPP_NUMBER}`;
const whatsappLink = `https://wa.me/${numero}?text=${encodeURIComponent("Hola, me interesa recibir más información.")}`;
---

<section class="curso-wrapper">
  <div class="curso-grid">
    <!-- Columna izquierda -->
    <div class="columna">
      <div class="titulo">¿De qué va este curso?</div>

      <div class="card-y-botones">
        <div class="card-wrapper">
          <div class="card-limit" data-modal-open="modal-curso">
            <img
              src="/limit.svg"
              alt="Cupos limitados"
              class="limit-img"
              loading="lazy"
            />
            <img
              src="/equipo.svg"
              alt="Equipo"
              class="equipo-img"
              loading="lazy"
            />
          </div>
        </div>

        <div class="botones">
          <button class="contenido" data-modal-open="modal-contenido">
            <span class="text-default">Ver contenido</span>
            <span class="text-hover">Del sesgo a la estrategia</span>
          </button>

          <!-- Este bloque aún no tiene comportamiento -->
          <!-- TODO: Conectar con inscripción cuando esté disponible -->

          <a
            href={whatsappLink}
            target="_blank"
            rel="noopener noreferrer"
            class="inscripcion"
            data-wa-text="Hola, quiero la prueba gratuita de este curso"
          >
            <span class="text-default">Inscríbete gratis</span>
            <span class="text-hover">Accede al primer módulo gratis</span>
            <!-- 
    🛠️ NOTA PARA DESARROLLADOR:
    Este botón eventualmente debe llevar al formulario de inscripción o a una página dedicada.
    
    OPCIONES FUTURAS:
    1. Reemplazar este <button> por un <a href="/ruta-a-formulario" class="inscripcion"> si el destino es una página nueva.
    2. Usar JavaScript para abrir un modal o disparar lógica de inscripción si es un formulario embebido.
    
    POSIBLE RUTA:
    - /inscripcion/liderazgo
    - /registro?curso=liderazgo-persuasion
    
    Cuando esté disponible el formulario, cambiar este botón por:
    <a href="/inscripcion/liderazgo" class="inscripcion">...</a>
  -->
          </a>
        </div>
        <div class="titulo-h1">
          <p><strong>Liderazgo y Persuasión</strong></p>
          <p class="texto-h1">
            Descubre cómo motivar y movilizar equipos con ciencia y estrategia.
          </p>
        </div>

        <div class="competencias">
          <p><strong>Competencias Clave:</strong></p>
          <button
            class="soft open-modal-btn"
            data-modal-open="modal-habilidades">Soft Skills</button
          >
          <button class="hard open-modal-btn" data-modal-open="modal-hardskills"
            >Hard Skills</button
          >
          <a
            href={whatsappLink}
            target="_blank"
            rel="noopener noreferrer"
            class="all"
            data-wa-text="Hola, quiero la version completa de este curso"
          >
            QUIERO LA VERSIÓN COMPLETA DE ESTE CURSO
          </a>
        </div>
      </div>
    </div>

    <!-- Columna derecha -->
    <div class="columna">
      <div class="titulo">Liderazgo y Persuasión</div>
      <ul>
        <li>
          <span class="check"></span> En el primer módulo entenderás por qué a veces
          un mensaje bien intencionado genera confusión o resistencia.
        </li>
        <li>
          <span class="check"></span> Descubrirás cómo los sesgos mentales afectan
          la forma en que las personas interpretan lo que dices (¡incluso cuando
          tú crees que estás siendo claro!).
        </li>
        <li>
          <span class="check"></span> Luego aprenderás a usar principios de la economía
          del comportamiento para que tus mensajes no solo informen… sino inspiren
          acción inmediata.
        </li>
        <li>
          <span class="check"></span> Porque no basta con hablar: necesitas generar
          el sentido de urgencia correcto para movilizar decisiones.
        </li>
        <li>
          <span class="check"></span> A medida que avances, verás cómo cambiar la
          forma de decir algo puede marcar la diferencia entre la apatía y la motivación.
        </li>
        <li>
          <span class="check"></span> Finalmente, conectarás todo lo aprendido para
          diseñar mensajes que movilicen a tu equipo sin forzar nada, solo con claridad,
          estrategia y humanidad.
        </li>
      </ul>
    </div>
  </div>
</section>

<div class="modal" data-modal-id="modal-curso">
  <div class="modal-content">
    <span class="close" data-modal-close>&times;</span>
    <h2 class="modal-titulo">
      ¿Por qué hay cupos limitados en un curso que haces a tu ritmo?
    </h2>
    <p class="modal-parrafo">
      Aunque este curso es autodirigido, no se trata solo de consumir contenido.
      Para aprobarlo, deberás enfrentar situaciones simuladas y demostrar cómo
      actuarías aplicando lo aprendido.
    </p>
    <p class="modal-parrafo">
      Tu desempeño será evaluado por nuestro equipo, y recibirás
      retroalimentación personalizada para mejorar aún más tus habilidades.
    </p>
    <p class="modal-parrafo">
      Si superas esta etapa, podrás solicitar que validemos públicamente tus
      competencias en LinkedIn. Pero este proceso es manual, no automático, y
      por eso los cupos son limitados.
    </p>
    <p class="modal-strong">
      <strong>¿Te atreves a demostrar de lo que eres capaz?</strong>
    </p>
  </div>
</div>
<!-- ✅ Cierre correcto del primer modal -->

<!-- Segundo modal: Contenido -->
<div class="modal" data-modal-id="modal-contenido">
  <div class="modal-content">
    <button class="close" data-modal-close>&times;</button>

    <!-- Imagen representativa -->
    <img
      src="modulos.webp"
      alt="Contenido del Curso"
      class="modal-img"
      loading="lazy"
    />

    <!-- Botones -->
    <div class="modal-buttons">
      <a href={whatsappLink} onclick="event.preventDefault();" class="inscribirme" data-wa-text="Hola, quiero la prueba gratuita de este curso"
        >Inscribirme gratis</a
      >
      <button class="cerrar-modal" data-modal-close>Cerrar</button>
    </div>
  </div>
</div>

<!-- Tercer modal: Habilidades blandas -->
<div class="modal" data-modal-id="modal-habilidades">
  <div class="modal-content">
    <span class="close" data-modal-close>&times;</span>

    <!-- Imagen descriptiva del contenido -->
    <img
      src="soft.webp"
      alt="Habilidades blandas"
      class="modal-img"
      loading="lazy"
    />

    <!-- Botones al pie -->
    <div class="modal-buttons">
      <a href={whatsappLink} onclick="event.preventDefault();" class="inscribirme" data-wa-text="Hola, quiero la prueba gratuita de este curso"
        >Inscribirme gratis</a
      >
      <button class="cerrar-modal" data-modal-close>Cerrar</button>
    </div>
  </div>
</div>

<!-- Modal: Hard Skills -->
<div class="modal" data-modal-id="modal-hardskills">
  <div class="modal-content">
    <span class="close" data-modal-close>&times;</span>

    <!-- Imagen representativa -->
    <img
      src="hard.webp"
      alt="Habilidades técnicas"
      class="modal-img"
      loading="lazy"
    />

    <!-- Botones -->
    <div class="modal-buttons">
      <a href={whatsappLink} onclick="event.preventDefault();" class="inscribirme" data-wa-text="Hola, quiero la prueba gratuita de este curso"
        >Inscribirme gratis</a
      >
      <button class="cerrar-modal" data-modal-close>Cerrar</button>
    </div>
  </div>
</div>

<script is:inline>
  document.addEventListener("DOMContentLoaded", () => {
    const body = document.body;
    const numero = `${numero}`; // 🔁 esto inserta el número real al generar el HTML

    // Abrir modal
    document.querySelectorAll("[data-modal-open]").forEach((trigger) => {
      trigger.addEventListener("click", () => {
        const id = trigger.getAttribute("data-modal-open");
        const modal = document.querySelector(`[data-modal-id="${id}"]`);
        if (modal) {
          modal.style.display = "flex";
          body.classList.add("modal-open"); // bloquear fondo
        }
      });
    });

    // Cerrar modal por botón o click fuera
    document.querySelectorAll("[data-modal-close]").forEach((btn) => {
      btn.addEventListener("click", () => {
        const modal = btn.closest(".modal");
        if (modal) {
          modal.style.display = "none";
          body.classList.remove("modal-open"); // liberar fondo
        }
      });
    });

    // Cerrar si se hace clic fuera del contenido
    document.querySelectorAll(".modal").forEach((modal) => {
      modal.addEventListener("click", (e) => {
        if (e.target === modal) {
          modal.style.display = "none";
          body.classList.remove("modal-open");
        }
      });
    });

    // WhatsApp con texto dinámico
    document.querySelectorAll(".inscribirme").forEach((btn) => {
      btn.addEventListener("click", (event) => {
        event.preventDefault();
        const mensaje =
          btn.getAttribute("data-wa-text") ||
          "Hola, me interesa recibir más información.";
        const url = `https://wa.me/${numero}?text=${encodeURIComponent(mensaje)}`;
        window.open(url, "_blank");
      });
    });
  });
</script>

<style>
  .curso-wrapper {
    background-color: #1a1919;
    min-height: 100vh;
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .curso-grid {
    width: 90vw;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10vw;
    padding-top: 8vw;
  }

  .columna {
    border-radius: 10px;
    font-size: clamp(16px, 2.5vw, 1.5rem);
  }

  .titulo {
    font-family: "Arsenal SC", sans-serif;
    font-size: 2rem;
    font-weight: bold;
    margin-bottom: 2rem;
  }

  .titulo-h1 {
    max-width: 600px;
    font-family: "Arsenal SC", sans-serif;
    font-size: 1.8rem;
    text-align: center;
  }

  .texto-h1 {
    margin-top: 0.5rem;
    margin-bottom: 1rem;
    font-family: "Dosis", sans-serif;
    font-size: clamp(16px, 2.5vw, 1.5rem);
  }

  .card-y-botones {
    display: flex;
    flex-direction: column;
    align-items: center; /* ya lo tienes */
    gap: 1rem;
    width: 100%;
    max-width: 600px;
  }

  .card-wrapper {
    background: #dcdcdc;
    padding: 1rem;
    border-radius: 10px;
    box-shadow:
      inset 4px 4px 8px #b8b8b8,
      inset -4px -4px 8px #ffffff;
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    width: 100%;
  }

  .card-limit {
    background: #e0e0e0;
    border-radius: 16px;
    box-shadow:
      6px 6px 12px #b8b9be,
      -6px -6px 12px #ffffff;
    text-align: center;
    display: flex;
    flex-direction: column;
    margin: 0.8rem;
    cursor: pointer; /* Cambia cursor para indicar que se puede hacer clic */
    transition:
      transform 0.3s ease,
      box-shadow 0.3s ease,
      background-color 0.3s ease;
  }

  .card-limit:hover {
    transform: translateY(-4px) scale(1.02);
    box-shadow:
      0 8px 16px rgba(255, 255, 255, 0.2),
      0 4px 8px rgba(0, 0, 0, 0.3);
    background-color: #f5f5f5; /* Opcional: un toque más claro */
  }

  .botones,
  .competencias {
    width: 100%;
    display: flex;
    gap: 10px;
  }

  .botones button {
    flex: 1;
    height: 50px;
  }

  .botones button,
  .competencias button,
  .competencias a {
    transition:
      transform 0.3s ease,
      box-shadow 0.3s ease;
    cursor: pointer;
  }

  .botones button:hover,
  .competencias button:hover,
  .competencias a:hover {
    transform: translateY(-3px) scale(1.03);
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
  }

  .competencias {
    text-align: center;
    flex-direction: column;
  }

  .competencias p {
    font-family: "Arsenal SC", sans-serif;
  }

  .competencias button {
    width: 100%; /* hace que usen el mismo ancho que .botones */
    height: 50px;
    font-family: "Dosis", sans-serif;
    font-size: 1.4rem;
    font-weight: bold;
    border-radius: 6px;
    border: none;
    color: white;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .contenido,
  .inscripcion {
    flex: 1;
    padding: 8px;
    font-weight: bold;
    font-family: "Dosis", sans-serif;
    font-size: clamp(14px, 2.5vw, 20px);
    font-weight: bold;
    border-radius: 6px;
    border: none;
    color: white;
    cursor: pointer;
    transition: all 0.3s ease;
    box-sizing: border-box;
  }

  .contenido {
    position: relative;
    overflow: hidden;
    background-color: #8c0303;
    box-shadow: inset 5px 5px 4px 4px rgba(246, 159, 169, 0.6);
    border: none;
    color: white;
    cursor: pointer;
    /*padding: 0.8rem 2rem;*/
    transition: all 0.3s ease;
    white-space: nowrap;
  }

  .contenido .text-hover {
    display: none;
    font-size: 1rem;
  }

  .contenido:hover .text-default {
    display: none;
  }

  .contenido:hover .text-hover {
    display: inline;
  }

  .inscripcion {
    position: relative;
    overflow: hidden;
    font-weight: bold;
    font-family: "Dosis", sans-serif;
    font-size: clamp(14px, 2.5vw, 20px);
    text-align: center;
    text-decoration: none;
    background-color: #615d5d;
    box-shadow: inset 5px 5px 4px 4px rgba(255, 255, 255, 0.3);
    border: none;
    color: white;
    cursor: pointer;
    /*padding: 0.8rem 1rem;*/
    transition: all 0.3s ease;
    white-space: nowrap;
  }

  .inscripcion .text-hover {
    display: none;
    font-size: 1rem;
  }

  .inscripcion:hover .text-default {
    display: none;
  }

  .inscripcion:hover .text-hover {
    display: inline;
  }

  .soft,
  .hard,
  .all {
    width: 100%;
    height: 35px;
    border-radius: 6px;
    border: none;
    color: white;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .soft,
  .hard {
    background-color: #615d5d;
    box-shadow: inset 5px 5px 4px 4px rgba(255, 255, 255, 0.3);
  }

  .all {
    font-family: "Dosis", sans-serif;
    padding-top: 10px;
    background-color: #8c0303;
    box-shadow: inset 5px 5px 4px 4px rgba(246, 159, 169, 0.6);
    text-decoration: none;
  }

  .soft:hover,
  .hard:hover,
  .all:hover {
    filter: brightness(1.1);
  }

  .check {
    display: inline-block;
    width: 20px; /* ancho del icono */
    aspect-ratio: 1 / 1;
    height: 20px; /* alto del icono */
    margin-right: 8px; /* separación entre icono y texto */
    background-image: url("list.svg");
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
    vertical-align: middle;
  }

  ul {
    list-style: none;
    padding-left: 0;
    font-family: "Dosis", sans-serif;
    font-size: clamp(14px, 2.5vw, 20px);
  }

  li {
    margin-bottom: 1.8rem;
  }

  .modal {
    display: none;
    position: fixed;
    inset: 0;
    background-color: rgba(0, 0, 0, 0.6);
    justify-content: center;
    align-items: center;
    z-index: 999;
    padding: 20px; /* Permite separación en pantallas pequeñas */
    overflow-y: auto;
  }

  .modal-content {
    background: white;
    max-height: 90vh;
    overflow-y: auto;
    padding: 2rem;
    border-radius: 12px;
    width: 90%;
    max-width: 600px;
    box-sizing: border-box;
    position: relative;
  }

  /* ✅ BOTÓN CERRAR SIEMPRE VISIBLE ARRIBA A LA DERECHA */
  .modal-content .close {
    position: absolute;
    top: 16px;
    right: 16px;
    font-size: 1.8rem;
    background: none;
    border: none;
    color: #555;
    cursor: pointer;
    z-index: 10;
  }

  .modal .close:hover {
    background-color: #f0f0f0;
  }

  .modal-titulo {
    font-family: "Arsenal SC", sans-serif;
  }

  .modal-parrafo,
  .texto-contenido {
    font-family: "Dosis", sans-serif;
    font-size: clamp(14px, 2.5vw, 20px);
  }

  .modal-img {
    max-width: 100%;
    height: auto;
    border-radius: 8px;
    margin-top: 2rem;
    margin-bottom: 2rem;
  }

  /* Botones al pie del modal */
  .modal-buttons {
    display: flex;
    flex-direction: row;
    justify-content: center;
    gap: 1rem;
    margin-top: 1.5rem;
    flex-wrap: wrap;
  }

  .modal-buttons .cerrar-modal,
  .modal-buttons .inscribirme {
    flex: 1 1 250px; /* iguales, mínimo 250px, crecen juntos */
    padding: 0.8rem 1.2rem;
    border-radius: 6px;
    font-weight: bold;
    text-align: center;
    font-family: "Dosis", sans-serif;
    font-size: clamp(14px, 2.5vw, 20px);
    font-size: 1rem;
    box-sizing: border-box;
  }

  /* Botón gris de cerrar */
  .modal-buttons .cerrar-modal {
    background-color: #615d5d;
    box-shadow: inset 5px 5px 4px 4px rgba(255, 255, 255, 0.3);
    color: white;
    border: none;
    padding: 0.6rem 1.2rem;
    border-radius: 4px;
    cursor: pointer;
  }

  /* Botón rojo de inscribirme */
  .modal-buttons .inscribirme {
    background-color: #8c0303;
    box-shadow: inset 5px 5px 4px 4px rgba(246, 159, 169, 0.6);
    color: white;
    text-decoration: none;
    padding: 0.6rem 1.2rem;
    border-radius: 4px;
    font-weight: bold;
  }

  .modal-buttons .cerrar-modal,
  .modal-buttons .inscribirme {
    transition:
      background-color 0.3s ease,
      color 0.3s ease,
      transform 0.2s ease;
  }

  /* Hover animado para botón gris */
  .modal-buttons .cerrar-modal:hover {
    background-color: #a3a1a1;
    color: black;
    transform: scale(1.03);
  }

  /* Hover animado para botón rojo */
  .modal-buttons .inscribirme:hover {
    background-color: #f49696;
    color: black;
    transform: scale(1.03);
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(-30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 1024px) {
    .curso-grid {
      grid-template-columns: 1fr;
    }

    .card-y-botones {
      align-items: center;
    }

    .botones,
    .competencias {
      flex-direction: column;
      align-items: center;
      width: 100%;
    }

    .contenido,
    .inscripcion,
    .competencias button {
      width: 100%;
      max-width: 300px;
      flex-grow: 0;
      height: 40px;
      font-size: 15px;
    }

    .titulo-h1 {
      width: 100%;
      max-width: 300px;
      margin: 0 auto;
      text-align: center;
      font-size: 18px;
    }

    .texto-h1 {
      font-size: 15px;
      line-height: 1.4;
      height: auto;
      box-sizing: border-box;
      margin: 0 0 1rem 0;
      padding: 0;
      text-align: center;
    }

    .card-wrapper {
      max-width: 300px;
    }
  }

  @media (max-width: 768px) {
    .curso-grid {
      grid-template-columns: 1fr;
    }

    .card-y-botones {
      align-items: center;
    }

    .botones,
    .competencias {
      flex-direction: column;
      align-items: center;
      width: 100%;
    }

    .competencias {
      margin-top: 2rem;
    }

    .contenido,
    .inscripcion,
    .competencias button {
      width: 100%;
      max-width: 300px;
      flex-grow: 0;
      height: 40px;
      font-size: 15px;
    }

    .titulo-h1 {
      width: 100%;
      max-width: 300px;
      margin: 0 auto;
      text-align: center;
      font-size: 18px;
    }

    .texto-h1 {
      font-size: 15px;
      line-height: 1.4;
      height: auto;
      box-sizing: border-box;
      margin: 0 0 1rem 0;
      padding: 0;
      text-align: center;
    }

    .card-wrapper {
      max-width: 300px;
    }
  }
</style>
