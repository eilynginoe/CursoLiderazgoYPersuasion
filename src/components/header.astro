<header>
  <section class="header-container">
    <!-- IZQUIERDA: Logo -->
    <div class="logoContainer">
      <a href="/" class="logo-link">
        <div class="image-container">
          <img
            class="imgLogo"
            loading="lazy"
            src="./favicon.svg"
            alt="Logo Leanind consulting"
          />
          <img
            class="imgLogo-letras"
            loading="lazy"
            src="./logo-letras.svg"
            alt="Letras del logo"
          />
        </div>
      </a>
    </div>

 <!-- DERECHA: Usuario y botón -->
    <div class="header-derecha">
      <div class="usuario-info">
        <span class="bienvenida">Bienvenido/a <strong class="usuario">Usuario/a!</strong></span>
        <span class="subtexto">¡Disfruta del modo de prueba!</span>
      </div>
      <a href="https://landing-page-comunicacion-persuasiv.vercel.app/" class="salir">
        SALIR <img src="/salir.svg" alt="salir" class="icono-salida" loading="lazy"/>
      </a>
    </div>
  </section>
</header>

<style>
  header {
    position: sticky;
    top: 0;
    width: 100%;
    height: 55px;
    background-color: white;
    font-family: "Dosis", sans-serif;
    z-index: 1000;
    box-sizing: border-box;
    box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.1);
  }

  .header-container {
    margin: 0 auto;
    height: 100%;
    padding: 0 20px;
    display: flex;
    align-items: center;
    justify-content: space-between; /* 👈 SEPARA IZQUIERDA y DERECHA */
  }

  .logoContainer .image-container {
    display: flex;
    align-items: center;
    gap: 5px;
  }

  .imgLogo {
    height: 60px;
    transition: transform 0.3s ease;
  }

  .imgLogo-letras {
    height: 50px;
     aspect-ratio: auto 200 / 80;
     width: auto;
    transition: transform 0.3s ease;
  }

  .logo-link {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    text-decoration: none;
    padding: 5px;
  }

  .logo-link:hover .imgLogo {
    transform: scale(1.1) rotate(10deg);
    filter: drop-shadow(0 4px 4px rgba(0, 0, 0, 0.3));
  }

  .header-derecha {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    text-align: right;
  }

  .usuario-info {
    display: flex;
    flex-direction: column;
    font-size: 0.9rem;
    line-height: 1.2;
  }

  .usuario {
    color: red;
    font-weight: bold;
  }

  .subtexto {
    font-size: 0.8rem;
    color: #333;
  }

  .salir {
    display: flex;
    align-items: center;
    text-decoration: none;
    color: #600000;
    font-weight: bold;
    font-size: 0.9rem;
  }

  .icono-salida {
    width: 16px;
    height: 16px;
    margin-left: 0.3rem;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .imgLogo {
      height: 50px;
    }

    .imgLogo-letras {
      height: 40px;
    }

    .usuario-info {
      font-size: 0.8rem;
    }

    .subtexto {
      font-size: 0.7rem;
    }
  }

  @media (max-width: 480px) {
    .imgLogo {
      height: 45px;
    }


    .header-derecha {
      flex-direction: column;
      align-items: flex-end;
      gap: 0.3rem;
    }
  }
</style>

